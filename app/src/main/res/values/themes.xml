<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MyApplication" parent="android:Theme.DeviceDefault.NoActionBar" />

    <!-- Tema splash — pakai ikon aplikasi yang sudah ada (ic_launcher_foreground),
         dengan latar gelap senada tema aplikasi. postSplashScreenTheme menentukan
         tema yang dipakai begitu splash selesai dan Activity utama tampil. -->
    <style name="Theme.ManuScribe.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.MyApplication</item>
    </style>
</resources>
